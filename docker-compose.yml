services:
  web:
    build: .
    ports:
      - "5000:5000"
    environment:
      SECRET_KEY: "change-me"
      TELEGRAM_BOT_TOKEN: ""
      TELEGRAM_CHAT_ID: ""
    volumes:
      - ./instance:/app/instance
